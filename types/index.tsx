export interface CartItemProps {
  productName: string;
  price: number;
  available: boolean;
  quantity: number;
}

export interface SliderTextProps {
  title: string;
  description: string;
  url: string;
}
