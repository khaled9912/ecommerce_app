export interface SliderTextProps {
  title: string;
  description: string;
  url: string;
}

export interface ProductProps {
  id: string;
  title: string;
  price: number;
  category: string;
  description: string;
  image: string;
  quantity: number;
}

export interface CategoryItemProps {
  name: string;
  imgUrl: string;
}
